apiVersion: v1
kind: Service
metadata:
  name: {{ include "datachat.fullname" . }}
  labels:
    {{- include "datachat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "datachat.selectorLabels" . | nindent 4 }}
