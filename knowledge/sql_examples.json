[
  {
    "question": "How many columns are in table silver.s_el_ballbesitze?",
    "sql": "SELECT table_schema, table_name, count(*)::integer AS column_count\nFROM information_schema.columns\nWHERE table_schema = 'silver' AND table_name = 's_el_ballbesitze'\nGROUP BY table_schema, table_name",
    "tables": [
      "silver.s_el_ballbesitze"
    ],
    "tags": [
      "schema",
      "columns",
      "metadata"
    ],
    "doc": "Use information_schema.columns for exact column counts. Do not list all tables when the user names one table."
  },
  {
    "question": "Show columns in table bronze.b_el_boxscore",
    "sql": "SELECT table_schema, table_name, column_name, data_type, ordinal_position\nFROM information_schema.columns\nWHERE table_schema = 'bronze' AND table_name = 'b_el_boxscore'\nORDER BY ordinal_position",
    "tables": [
      "bronze.b_el_boxscore"
    ],
    "tags": [
      "schema",
      "columns",
      "metadata"
    ],
    "doc": "For column-list questions, return column metadata ordered by ordinal_position."
  },
  {
    "question": "Show 20 sample rows from bronze.b_el_boxscore",
    "sql": "SELECT *\nFROM bronze.b_el_boxscore\nLIMIT 20",
    "tables": [
      "bronze.b_el_boxscore"
    ],
    "tags": [
      "sample",
      "rows",
      "preview"
    ],
    "doc": "For sample rows, return raw rows with a small LIMIT. Do not aggregate."
  },
  {
    "question": "Top 20 EuroLeague players by total points, from bronze.b_el_boxscore",
    "sql": "SELECT player_name, SUM(pts) AS total_pts\nFROM bronze.b_el_boxscore\nWHERE pts IS NOT NULL\nGROUP BY player_name\nHAVING SUM(pts) > 0\nORDER BY total_pts DESC\nLIMIT 20",
    "tables": [
      "bronze.b_el_boxscore"
    ],
    "tags": [
      "chart",
      "bar",
      "euroleague",
      "points",
      "player"
    ],
    "doc": "For top scoring questions, group by player_name, sum pts, remove zero totals, order by total_pts, and limit results."
  },
  {
    "question": "Top EuroCup players by total points",
    "sql": "SELECT player_name, SUM(pts) AS total_pts\nFROM bronze.b_ec_boxscore\nWHERE pts IS NOT NULL\nGROUP BY player_name\nHAVING SUM(pts) > 0\nORDER BY total_pts DESC\nLIMIT 20",
    "tables": [
      "bronze.b_ec_boxscore"
    ],
    "tags": [
      "chart",
      "bar",
      "eurocup",
      "points",
      "player"
    ],
    "doc": "EuroCup bronze tables use the b_ec prefix."
  },
  {
    "question": "pts vs minutes from bronze.b_bbl_boxscore, using only rows where minutes is not null",
    "sql": "SELECT split_part(minutes, ':', 1)::numeric + split_part(minutes, ':', 2)::numeric / 60.0 AS minutes,\n       pts,\n       player_name,\n       player_team\nFROM bronze.b_bbl_boxscore\nWHERE minutes ~ '^[0-9]{1,3}:[0-9]{2}$'\n  AND minutes <> '00:00'\n  AND pts IS NOT NULL\nLIMIT 500",
    "tables": [
      "bronze.b_bbl_boxscore"
    ],
    "tags": [
      "scatter",
      "minutes",
      "points",
      "bbl"
    ],
    "doc": "Boxscore minutes are text in MM:SS format. Convert them to decimal minutes before charting."
  },
  {
    "question": "Compare EuroLeague player scoring efficiency from silver.s_el_player_stats_prospiel and gold.g_el_players",
    "sql": "SELECT s.player_name,\n       s.player_team,\n       COUNT(*) AS games_played,\n       SUM(s.pts) AS total_pts,\n       ROUND(AVG(s.pts)::numeric, 2) AS avg_pts_per_game\nFROM silver.s_el_player_stats_prospiel AS s\nGROUP BY s.player_name, s.player_team\nHAVING COUNT(*) >= 5\nORDER BY total_pts DESC\nLIMIT 25",
    "tables": [
      "silver.s_el_player_stats_prospiel",
      "gold.g_el_players"
    ],
    "tags": [
      "silver",
      "gold",
      "euroleague",
      "efficiency",
      "points",
      "player"
    ],
    "doc": "When a join key is uncertain, prefer a reliable silver aggregation over inventing a fragile join. Use gold only when column names clearly match."
  },
  {
    "question": "How many rows are in each bronze table?",
    "sql": "SELECT table_schema, table_name\nFROM information_schema.tables\nWHERE table_schema = 'bronze' AND table_type = 'BASE TABLE'\nORDER BY table_name",
    "tables": [
      "bronze"
    ],
    "tags": [
      "row",
      "count",
      "metadata",
      "bronze"
    ],
    "doc": "Dynamic per-table row counts need generated SQL because table names cannot be parameterized as values."
  },
  {
    "question": "Which players have the most rows in bronze.b_el_boxscore?",
    "sql": "SELECT player_name, COUNT(*) AS row_count\nFROM bronze.b_el_boxscore\nGROUP BY player_name\nORDER BY row_count DESC\nLIMIT 25",
    "tables": [
      "bronze.b_el_boxscore"
    ],
    "tags": [
      "count",
      "player",
      "euroleague",
      "rows"
    ],
    "doc": "Use COUNT(*) for row-frequency questions, not SUM(pts)."
  },
  {
    "question": "Who made the most three-pointers in EuroLeague season 2025-2026?",
    "sql": "SELECT player_name, SUM(three_p) AS threes_made\nFROM bronze.b_el_boxscore\nWHERE date >= '2025-08-01' AND date < '2026-08-01'\nGROUP BY player_name\nORDER BY threes_made DESC\nLIMIT 10",
    "tables": [
      "bronze.b_el_boxscore"
    ],
    "tags": [
      "season",
      "2025-2026",
      "three_p",
      "date",
      "euroleague",
      "eurocup"
    ],
    "doc": "bronze tables have no season column, only date - a query for a named season MUST filter date to that season's range (August of the first year through July of the next, since the competition year crosses the calendar year boundary) or it silently mixes every season together. GROUP BY player_name alone, not (player_name, player_team): mid-season sponsor renames give the same player two team-name rows in one season."
  }
]
